# Don't touch : Settings generated by Ansile
parameters:
    kernel.logs_dir: {{ log_project_file }}_{{ app_env }}
    env(MAILER_DSN): {{ MAILER_DSN }}
    env(LOCAL_DATABASE_URL): {{ mysql_url }}
    env(REMOTE_DATABASE_URL): {{ backup_url }}


