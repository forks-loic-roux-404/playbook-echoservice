# Don't touch : Settings generated by Ansile
parameters:
    kernel.logs_dir: {{ log_project_file }}_{{ app_env }}

doctrine:
    dbal:
        dbname:                {{ mysql_db }}
        host:                  {{ mysql_host }}
        port:                  {{ mysql_port }}
        user:                  {{ mysql_user }}
        password:              {{ mysql_pw }}

framework:
    mailer:
        dsn: {{ MAILER_DSN }}