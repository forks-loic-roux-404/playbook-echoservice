### Install basic system packages
- name: Install basic packages
  apt: 
    pkg:
      - gnupg2
      - vim
      - git
      - curl
      - wget
      - zip
      - unzip
      - htop
      - iotop
      - postfix
      - apt-transport-https
      - ca-certificates
      - imagemagick
    state: present

### Convenience
- name: Configure inputrc / vimrc and motd for root
  become: yes
  template: 
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
  with_items:
   - { src: inputrc, dest: /root/.inputrc }
   - { src: vimrc, dest: /root/.vimrc }
   - { src: inputrc, dest: /home/vagrant/.inputrc }
   - { src: vimrc, dest: /home/vagrant/.vimrc }
   - { src: motd, dest: /etc/motd }
   - { src: bashrc, dest: /home/vagrant/.bashrc }
   - { src: aliases, dest: /home/vagrant/.bash_aliases, force: "{{ update_aliases }}" }

- name: Edit some configs
  lineinfile:
    path: "{{ item.path }}"
    regexp: "{{ item.regexp }}"
    line: "{{ item.line }}"
  with_items:
    - { path: '/etc/ssh/sshd_config', regexp: '^#PrintLastLog yes', line: PrintLastLog no }
    - { path: '/etc/ssh/sshd_config', regexp: '^#PermitRootLogin prohibit-password', line: "PermitRootLogin {{ permit_root_login }}" }
    #- { path: '/proc/sys/vm/overcommit_memory', '^0', line: "vm.overcommit_memory={{ over_commit_memory }}" }
  #ignore_errors: yes

- name: Set new welcome message  
  file:
    path: /etc/update-motd.d/
    state: absent
  become: yes

- name: Create logs dir
  file:
    path: "{{ log_project_dir }}"
    state: directory
    recurse: yes
    owner: vagrant
    group: vagrant
    mode: 750
